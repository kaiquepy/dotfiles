# cd
alias ..="cd .."

# ls
alias ls='echo && ls --color=always --group-directories-first'

# ip
alias ip='ip -c'

# Simple copy/past clipboard
alias fromclip='xclip -o -sel copy'
alias toclip='xclip -i -sel copy'
